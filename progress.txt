PROGRESS ACHEIVED IN THE PROJECT
1. So far we have done:
   * Taking input as excel sheets and generated an excel sheet.
   * We have caculated values and inserted into cells directly.
   * We imported dates from epics file comparing Custom field(Epic link). 
2. Progress we have achieved:
   * Taking csv files as inputs and generated output in excel format.
   * Inserted data bar in the place of STORY EFFORT COMPLETION % column.
   * Formulae are inserted in the place of values.
   * Inserted new row to give Heading.
   * Filled colors using PatternFill.
   * Cleared all pylint errors.
